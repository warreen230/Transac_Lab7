@model ZombieParty.Models.ZombieVM
@{
    ViewData["Title"] = "Delete";
}
<h1>Delete</h1>
<h4>Zombie</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <div class="row p-3">
            <form asp-action="DeletePost" asp-route-id="@Model.Zombie.Id">
                
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group row">
                    <input asp-for="@Model.Zombie.Id" class="form-control" hidden />
                    <div class="col-4">
                        <label asp-for="@Model.Zombie.Name" class="control-label"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="@Model.Zombie.Name" class="form-control" />
                        <span asp-validation-for="@Model.Zombie.Name" class="text-danger"></span>
                    </div>
                </div>
     
                <div class="form-group">
                    <input type="submit" value="Delete" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
